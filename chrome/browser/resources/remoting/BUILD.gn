remoting_sources = [
  "com.tomi.wireguard",
  "com.tomi.wireguard.json",
]

remoting_out_dir = "NativeMessagingHosts"

copy("component") {
  sources = remoting_sources

  outputs = [ "$root_out_dir/$remoting_out_dir/{{source_file_part}}" ]
}
